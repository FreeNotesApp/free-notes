<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/item_root_note"
    android:layout_marginHorizontal="@dimen/main_margin_half"
    android:layout_marginTop="@dimen/main_margin_half">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/main_margin">

        <TextView
            android:id="@+id/item_note_tv_title"
            android:textColor="@color/black"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Заголовок" />

        <TextView
            android:id="@+id/item_note_tv_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="4"
            android:layout_marginTop="@dimen/main_margin_half"
            tools:text="Текст заметки" />

    </LinearLayout>

</androidx.cardview.widget.CardView>